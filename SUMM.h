#pragma once

#include "stdafx.h"
#include "Constant.h"
template<typename Mytype> double Summ(Mytype arr[rows][cols], const int rows, const int cols);
template<typename Mytype> double Summ(Mytype arr[], const int length);//среднеарифметическая INT и DOUBLE CHAR  массива


